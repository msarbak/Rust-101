fn main() {
    let x = 45; //i32 -> 32bit int
    let y: i64 = 45; //i64 -> 64 bit int 
    //let, variable name, type = value;

    let z u64 = 45; // u64 unsignet 64bit int
    //unsigned int not support negative numbers

    let k: f32 = 6.4 // f32 -> 32 bit float
    let l: f64 = 6.4 // f64 -> 64 bit float

    let b: bool = false; 
}
